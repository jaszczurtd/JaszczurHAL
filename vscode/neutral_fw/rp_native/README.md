# Native RP neutral identity firmware

This minimal Pico SDK application uses JaszczurHAL's default USB descriptors.
The `jh-vscode clear-identity` action builds and flashes it for the selected
native RP target and board.
