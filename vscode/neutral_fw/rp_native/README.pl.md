# Neutralny firmware identyfikacyjny native RP

Ta minimalna aplikacja Pico SDK używa domyślnych deskryptorów USB JaszczurHAL.
Akcja `jh-vscode clear-identity` buduje ją i wgrywa dla wybranego targetu oraz
boardu native RP.
