# Windows Runtime

The public CLI contract is shared with Linux and exposed through `entry/`.

Windows-specific implementations will live here. Until they are implemented,
Linux is the first-class runtime and Windows support is intentionally documented
as pending rather than silently drifting through copied templates.

