#include "hal_config.h"

/*
 * Network status-returning APIs now live in their owning backend/shared
 * implementations. This translation unit remains in the common source list for
 * existing build files.
 */
