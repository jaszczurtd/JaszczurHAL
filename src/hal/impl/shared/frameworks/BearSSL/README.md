# BearSSL integration

This directory contains the JaszczurHAL TLS provider and transport adapters.
The unmodified upstream BearSSL checkout is managed separately at
`third_party/BearSSL` from `third_party/bearssl_version.conf`.
