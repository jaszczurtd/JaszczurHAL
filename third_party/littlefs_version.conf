# JaszczurHAL pinned littlefs dependency.
#
# The shared LittleFS provider, its host RAM-flash integration test, and native
# RP/STM32G474 builds compile this exact upstream checkout. Upgrade
# LITTLEFS_REF and LITTLEFS_VERSION together, then run the component updater
# and the full project gate.
LITTLEFS_REPO=https://github.com/littlefs-project/littlefs.git
LITTLEFS_REF=6cb4e86540eca0d9ba62500a298385c9d863c8be
LITTLEFS_VERSION=2.11.3
LITTLEFS_DIR=third_party/littlefs
